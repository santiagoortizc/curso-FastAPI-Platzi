[project]
name = "fastapi_platzi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "pytest>=9.0.2",
    "sqlmodel>=0.0.37",
    "tzdata>=2025.3",
]
